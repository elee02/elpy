{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ src/*.cpp -o main",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}